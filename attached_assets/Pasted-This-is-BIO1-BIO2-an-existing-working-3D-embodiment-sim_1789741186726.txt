This is BIO1 / BIO2, an existing working 3D embodiment simulation. You are extending the existing project, NOT rebuilding it.

FIRST:
Read the entire existing project before changing anything, especially:

* index.html
* css/style.css
* js/brain.js
* js/app.js
* js/scan.js
* README.md
* The folder m and everything relevant inside it

Do not restructure the project or move it into a framework.

IMPORTANT:
Keep the existing simulation and neuroscience behavior intact. Do not rewrite the Brain system, behavior logic, physics, or agent architecture unless something is genuinely broken.

Remove/ignore the previously planned WebSocket, Node server, shared-state, Render, or authoritative-server implementation. Do not add backend infrastructure for this. The existing browser-based simulation should remain the actual experience and continue working as a normal Vercel-hosted frontend.

MAIN TASK: UI + 3D ENVIRONMENT UPGRADE

The biggest priority is making the actual BIO1/BIO2 experience look substantially more polished, immersive, and scientifically interesting.

Do NOT turn this into a game.

1. Upgrade the 3D environment

Improve the existing three.js arena and surrounding environment significantly while preserving the current simulation mechanics.

Make it feel like a sophisticated experimental observation environment rather than a basic three.js demo.

Improve things such as:

* arena geometry
* floor/material quality
* lighting
* shadows
* depth and spatial atmosphere
* obstacle presentation
* food/resource presentation
* environmental details
* BIO1/BIO2 visual quality
* wing, shell, eye, leg and body materials
* scale and spatial relationships
* camera composition
* subtle environmental animation
* overall visual hierarchy

Keep the environment relatively clean and restrained. Do NOT add excessive neon, gradients, particles, clutter, giant text, or generic “AI dashboard” aesthetics.

The simulation should remain easy to observe.

2. Major upgrade to the fly-brain 3D mapping visualization

There is an existing fly-brain/connectome visualization in the m folder.

This is a major priority.

Inspect what is already there and substantially improve the 3D representation.

The goal is to make it visually resemble a serious fruit-fly connectome / nervous-system mapping visualization.

It should feel inspired by actual biological connectome structure:

* dense but intelligible neural structures
* branching neuronal pathways
* realistic spatial organization
* meaningful depth
* layered neural regions
* individual nodes/connections where appropriate
* subtle anatomical structure
* convincing 3D volume
* smooth rotation/orbit
* restrained scientific visualization styling
* good lighting and depth cues
* high-quality rendering

Do NOT claim that the visualization is real EM/connectome scan data if the underlying data is procedural.

Keep the existing framing that the scan visualization is a procedural/generated representation and not real scan data.

However, make the visualization itself much more convincing and visually representative of a real fruit-fly connectome.

If the current m folder already contains useful geometry, data, rendering logic, or assets, improve and reuse them rather than replacing everything unnecessarily.

3. Integrate the brain visualization into the site

The fly-brain mapping should feel like an intentional part of the BIO1/BIO2 experience, not a random floating demo.

Improve its placement, panel design, sizing, typography, controls and relationship to the main 3D simulation.

The UI should feel like an experimental observation interface.

Avoid:

* game HUDs
* scores
* leaderboards
* XP
* missions
* player controls
* unnecessary buttons
* generic SaaS dashboard cards
* excessive text

The user should primarily observe the organisms and their environment.

4. Overall UI polish

Review the entire interface and improve the visual system where appropriate.

Focus on:

* typography
* spacing
* hierarchy
* panel composition
* responsive behavior
* desktop/mobile presentation
* subtle borders
* transparency/material treatment
* information density
* animation quality
* transitions
* visual consistency

Do not redesign the entire site for the sake of redesigning it.

Preserve useful existing UI and improve it.

5. Preserve the scientific/observational framing

This is an observation instrument.

There is:

* no player
* no score
* no winner
* no objective
* no game loop

Do not introduce any of these.

The interface should communicate that the viewer is observing two embodied biological agents and their environment.

6. Technical constraints

* Keep vanilla JS + three.js.
* Keep the existing file structure and naming conventions.
* Do not introduce React, Next.js, Vite, or another framework.
* Do not add a backend.
* Do not add WebSockets.
* Do not add Render infrastructure.
* Do not create a Node simulation server.
* Do not create a database for the simulation.
* Do not replace working systems unnecessarily.
* Keep it deployable directly to Vercel.
* Do not start unnecessary local servers or create unrelated development infrastructure.
* Do not modify unrelated functionality.

Before editing, understand how the current files communicate with each other.

After editing, make sure there are no broken imports, missing assets, undefined functions, console errors, or broken interactions.

Most importantly: do not sacrifice the existing BIO1/BIO2 simulation behavior while upgrading the visual experience.

The end result should feel like a significantly more sophisticated version of the existing project, especially the 3D environment and the fruit-fly connectome visualization, while still clearly being the same BIO1/BIO2 experiment.